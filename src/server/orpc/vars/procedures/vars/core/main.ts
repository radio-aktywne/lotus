import { bindings } from "./vars/bindings";
import { media } from "./vars/media";
import { playlists } from "./vars/playlists";

export const core = {
  bindings: bindings,
  media: media,
  playlists: playlists,
};
